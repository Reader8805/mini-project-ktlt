#include "get_data.hpp"

void convert_data(std::string &fileName, std::vector<std::string> &data) {
    std::vector<std::string> packet;
    packet.push_back("0xAA"); // push the start byte(1 byte)

    
    
}